part of '../../import.dart';

class FavoritePge extends StatelessWidget {
  const FavoritePge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('FavoritePge');
  }
}
